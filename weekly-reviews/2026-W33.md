# Weekly Review — 2026-W33

- Week: 2026-08-10 至 2026-08-16
- Time budget: 3–4 小时
- Status: 进行中

## 上周总结

- GPU Execution Model 达到掌握度 3，能够分析 Grid/Block/Warp/Thread、同 Stream Grid 顺序和跨 Stream Event 依赖。
- CUDA 异步执行与 Stream/Event 已完成真实 GPU 计时和 profiler 验证，达到掌握度 3。
- 跨 Stream Tensor 生命周期达到掌握度 3，能够分析 `record_stream()`、Storage alias、allocator pending block 和手动 Event；真实 GPU 实验仍缺失。
- GPU Memory Hierarchy 从未学习进入掌握度 2，已理解作用域、HBM 带宽、片上复用和跨 Block global-memory 共享。
- 上周主要风险是 GPU 基础主题密度较高，需要通过间隔复测巩固，避免把引导后的正确回答误判为独立掌握。

## 本周已完成

- 完成 Register、Shared Memory、L2、HBM 的典型延迟、容量和作用域排序。
- 能计算 Shared Memory 数据复用带来的 HBM 流量下降，并判断无复用时 staging 可能增加开销。
- 学习 global-memory coalescing，能够区分逻辑访存数量、物理内存事务和有效带宽。
- 经复测后能用 profiler 指标区分数据复用与访问重排。
- 初步学习 Shared Memory bank、同地址广播与同 bank 不同地址冲突。
- 保存完整 padded tiled-transpose 示例，并用 CPU 索引模拟验证非整 tile 边界。

## 本周计划

1. 从 `examples/cuda/tiled_transpose.cu` 闭卷复测输入/输出 tile 的 Block 坐标交换和 tile 内部转置。
2. 独立推导 `tile[32][32]` 与 `tile[32][33]` 按列读取时的 bank 编号，解释 padding 的作用。
3. 复测 GPU Execution Model、CUDA Event 计时和 Stream 并发，确认间隔后仍能独立分析。
4. 有 NVIDIA GPU 时，编译运行 tiled transpose，并用 profiler 比较 naive、unpadded、padded 版本。
5. 有 NVIDIA GPU 时，完成 `record_stream()` 三版本实验并保存地址复用和正确性证据。

## 进度估算

- Phase 1：约 65%。GPU 执行、异步 Stream/Event 和基础 profiler 已验证；Memory Hierarchy 已进入应用代码，但系统基础、真实 kernel 和 profiler 对比仍有缺口。
- 整个 24 周课程：约 11%。Phase 1 正在推进，Phase 2～6 尚未正式进入。

## 风险与调整

- tiled transpose 的两层坐标交换和 bank padding 尚依赖完整上下文，本周优先闭卷复测，不立即增加更多优化主题。
- 当前本机没有 `nvcc`；没有 GPU 环境时先完成代码推导，不把 CPU 索引模拟等同于 CUDA 实机验证。
- `record_stream()` 和 Memory Hierarchy 都需要 GPU 实验，租用环境时集中完成，减少环境准备成本。
